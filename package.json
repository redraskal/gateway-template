{
  "name": "template",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bunx gateway",
    "dev": "bunx gateway dev",
    "gen": "bunx gateway gen",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "gateway": "redraskal/gateway#main",
    "prettier": "^3.0.0"
  },
  "bun-create": {
    "postinstall": [
      "echo '`bun run dev` to start a development server.'",
      "echo '`bun start` to start a production server.'",
      "echo '`bun gen {route_name}` to create a new route file in pages.'"
    ],
    "start": "bun run dev"
  }
}
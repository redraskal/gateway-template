{
  "name": "template",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "gateway",
    "dev": "gateway dev",
    "gen": "gateway gen",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "gateway": "redraskal/gateway"
  }
}
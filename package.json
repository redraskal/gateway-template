{
  "name": "template",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bunx gateway",
    "dev": "bunx gateway dev",
    "gen": "bunx gateway gen",
    "prettier": "prettier --write ."
  },
  "dependencies": {
    "gateway": "redraskal/gateway",
    "prettier": "^3.0.0"
  }
}